# DO NOT COMMIT REAL VALUES.
# Install the real file at:
# /etc/scientiam/remote-ops/openai-tunnel.env
#
# Runtime API key with only the permissions required to USE the selected tunnel.
CONTROL_PLANE_API_KEY=REPLACE_WITH_RUNTIME_KEY
